# accept input and put on stack
DEF PUT:
	"Enter a number" println
	in
;

# search stack for input
DEF SEARCH:
	"Enter a number" println
	in
;

DEF CHECK_N:
	print
;

# main loop
DEF MAIN:
	"a to add, s to search, e to exit" println
	cin
	dup "a" = IF:
		drop PUT 0;
	ELSE: 
		dup "s" = IF: drop SEARCH 0;
		ELSE:
			dup "e" != IF: drop 0; ELSE: drop 1;
		;
	;
;

DO: MAIN; WHILE: != 1;
